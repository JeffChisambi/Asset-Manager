---
trigger: always_on
---

Before you create any new UI, ensure that you have made a full audit of how the current design looks like: colors, borders,animation,spacing, logic.
use that existing style precisely when adding new UIs.
